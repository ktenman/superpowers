.worktrees/
.private-journal/
.claude/
.superpowers/
.DS_Store
node_modules/
__pycache__/

# AKIT lexicon mirror: built locally by `skills/akit-lookup/akit.py update`.
# AKIT content is © Cybernetica AS and is not redistributed with this repo.
skills/akit-lookup/akit_raw.jsonl.gz
skills/akit-lookup/akit.jsonl
inspo
triage/

# Eval harness lives in its own repository, cloned into evals/ for local
# development (see CLAUDE.md / README.md). It is not part of the published
# plugin, so the whole directory is ignored here.
evals/
